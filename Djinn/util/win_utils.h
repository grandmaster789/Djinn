#pragma once

#include "preprocessor.h"

#if DJINN_PLATFORM == DJINN_PLATFORM_WINDOWS
// really this should include some detection for the MSVC compiler
// but I can't be bothered at this point

namespace djinn::util::detail {
    
}

#endif